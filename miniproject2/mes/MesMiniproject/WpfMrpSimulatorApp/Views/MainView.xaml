<mah:MetroWindow
    x:Class="WpfMrpSimulatorApp.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
        WindowStyle="ToolWindow"
        xmlns:local="clr-namespace:WpfMrpSimulatorApp.Views"
        mc:Ignorable="d"
        Title="MRP 공정관리" Height="450" Width="800" ResizeMode="NoResize" 
        MinWidth="471" MinHeight="290"
        Dialog:DialogParticipation.Register="{Binding}" Closing="MetroWindow_Closing">
    
    <mah:MetroWindow.IconTemplate>
        <DataTemplate>
            <iconPacks:PackIconVaadinIcons Kind="Storage" Foreground="White" 
                                           Margin="0,7,0,0" Width="24"/>
        </DataTemplate>
    </mah:MetroWindow.IconTemplate>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        
        <!-- UI 구성 중  상단메뉴 -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch">

                <Button Content="기본설정" FontSize="12" Background="#A5BEBB" Margin="5"
                        Command="{Binding AppSettingCommand}">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Label Content="{Binding}" Foreground="White" VerticalAlignment="Center" />
                            <iconPacks:PackIconVaadinIcons Kind="Cogs" Foreground="White"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>

                <Button Content="공정계획" FontSize="12" Background="#D9CBAF" Margin="5">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Label Content="{Binding}" Foreground="White" VerticalAlignment="Center" />
                            <iconPacks:PackIconVaadinIcons Kind="Calendar" Foreground="White"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>

                <Button Content="모니터링" FontSize="12" Background="#AB9E89" Margin="5">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Label Content="{Binding}" Foreground="White" VerticalAlignment="Center" />
                            <iconPacks:PackIconUnicons Kind="EyeLine" Foreground="White"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>

                <Button Content="공정보고" FontSize="12" Background="#084D57" Margin="5">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Label Content="{Binding}" Foreground="White" VerticalAlignment="Center" />
                            <iconPacks:PackIconUnicons Kind="FileGraphLine" Foreground="White"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>

            </StackPanel>

            <Button Grid.Column="1" Content="종료" FontSize="12" Background="#993D3D" Margin="5"
                    Command="{Binding AppExitCommand}">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Label Content="{Binding}" Foreground="White" VerticalAlignment="Center" />
                                <iconPacks:PackIconFontAwesome Kind="PowerOffSolid" Foreground="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

        </Grid>
        
        <!-- UI 구성 중  메인 컨텐츠 영역 -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentView}" />
        
        <!--UI 구성 중  하단 상태바 -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding}" />
            </StatusBarItem>
            <Separator Background="White"/>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding CurrentTime}"/>
            </StatusBarItem>
        </StatusBar>
        
        
        
    </Grid>
</mah:MetroWindow>